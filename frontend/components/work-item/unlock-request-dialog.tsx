/**
 * EP-17 G3 — Re-export barrel.
 * The implementation lives in components/locks/ to co-locate all lock UI.
 * This re-export keeps the work-item component tree path consistent.
 */
export { UnlockRequestDialog } from '@/components/locks/unlock-request-dialog';
