# Pending Re-Decisions

Living document. Contains decisions originally scoped as "MVP-only" shortcuts. The project is no longer framed as an MVP, so each shortcut below needs to be re-evaluated against the full-product scope.

**Workflow:** pick a row → discuss → decide → update the row to `RESOLVED` with date, outcome, and affected epics → propagate the resolution to `assumptions.md`, affected `tasks/EP-*/design.md`, and any other artifact.

**Status legend:** 🔴 open · 🟡 in discussion · 🟢 resolved

---

## Decisions

| # | Source | Topic | Original MVP shortcut | Impact if re-decided | Status |
|---|--------|-------|----------------------|----------------------|--------|
| 1 | `assumptions.md` Architecture | **Multi-workspace** | Single workspace per deployment; multi-workspace routing deferred | Routing, auth resolution, `workspace_id` scoping on every query, subdomain/slug strategy | 🟢 resolved 2026-04-14 — **multi-workspace per deployment**. Slug/subdomain routing. See decision A in conversation log. |
| 2 | `assumptions.md` Architecture | **Multi-tenancy at DB level** | Single-tenant; no workspace isolation in DB | Row-level security policies, `workspace_id` columns everywhere, partitioning strategy, backup per tenant | 🟢 resolved 2026-04-14 — **multi-tenant**. `workspace_id` on every domain table + PostgreSQL RLS. Triggers schema-wide review of FK + index strategy. |
| 3 | `assumptions.md` Auth | **Auth providers** | Google OAuth 2.0 only | SSO providers (Microsoft, Okta), SAML, email+password fallback | 🟢 resolved 2026-04-14 — **Google OAuth only**. Internal product, Tuio uses Google Workspace. No fallback, no SAML, no email/password. External users get temporary Tuio Google account if needed. |
| 4 | `assumptions.md` Search | **Search engine** | PostgreSQL FTS only, no Elasticsearch | Operational cost (Elastic cluster), relevance tuning, semantic layer (Puppet EP-13 coupling) | 🟢 resolved 2026-04-14 — **search direct to Puppet, NOT via Dundun**. Our BE pushes work_items / sections / comments / tasks to Puppet via Celery sync pipeline, tagged with `wm_<workspace_id>`. Searchbar calls our BE → Puppet API with tag filter. Dundun not in search path. SQL local handles filters/listings/ID/dashboards. No PG FTS. <3s eventual consistency accepted. |
| 5 | `assumptions.md` Jira | **Jira sync delivery** | Polling only, webhook deferred | Webhook receiver, signature verification, idempotency, webhook→polling fallback | 🟢 resolved 2026-04-14 — **no automatic sync at all**. No polling, no webhooks, no `sync_logs` table. Inbound data only via user-initiated import action (see #12/#26). |
| 6 | `assumptions.md` Open Q4 | **Multiple workspaces per user** | One per deployment | Onboarding UX, workspace switcher, cross-workspace identity resolution | 🟢 resolved 2026-04-14 — **yes, N per user**. `workspace_memberships` n:m, session `active_workspace_id`, UI switcher, capability check scoped to active workspace. |
| 7 | `assumptions.md` Open Q5 | **Target scale** | 10K work items, 100K tasks | Index strategy, search, dashboards, partitioning, pagination defaults — most of the arch decisions downstream depend on this | 🟢 resolved 2026-04-14 — **small internal product**. <100 users, <50 workspaces, <5GB/year. No partitioning, no read replicas, SQLAlchemy defaults, 2 Celery workers/queue, 256MB Redis, 7-day PITR. Full product feature scope regardless — scale doesn't gate features. No separate `capacity_planning.md` needed. |
| 8 | `assumptions.md` Open Q7 | **Email notifications** | Internal notifications only | SMTP/SES infra, bounce handling, unsubscribe, digest emails, template system | 🟢 resolved 2026-04-14 — **in-app only, no email ever**. No SMTP/SES, no bounce handling, no unsubscribe, no digest, no template system. Inbox + SSE is the whole delivery surface. |
| 9 | `tech_info.md` §8 #9 | **PG FTS vs Elasticsearch** | Sufficient at MVP scale | Same as #4 — consolidate | 🟢 resolved 2026-04-14 — consolidated with #4. No PG FTS, no Elasticsearch. Puppet is the only search backend. |
| 10 | `megadocumento` §3.3 | **Non-goals list** | "No real-time collab · No complex RBAC · No generic enterprise suite" declared as MVP non-goals | If full-product, are they still non-goals? Especially complex RBAC (already contradicted by 6 admin tiers) and real-time collab (contradicted by EP-17 edit locks) | 🟢 resolved 2026-04-14 — **collab**: async base + real-time presence (indicators, typing, "N viewing"); co-edition (CRDT/OT) stays non-goal. **RBAC**: capability array + 5 profiles as code constants (see #11). **Generic enterprise suite**: confirmed non-goal. |
| 11 | `megadocumento` §12.6 | **RBAC vs capabilities** | "No complex RBAC needed" | With 6 operational tiers already planned, need to decide: formal RBAC model OR keep capability array and document the mapping rigorously | 🟢 resolved 2026-04-14 — **capability array + 5 workspace profiles as code constants + Superadmin as platform flag**. `PROFILE_CAPABILITIES` dict has 5 workspace profiles (Member, Team Lead, Project Admin, Integration Admin, Workspace Admin). Superadmin is separate: `users.is_superadmin` boolean, seeded via config. Invite/edit UI only chooses a workspace profile, backend expands to `capabilities[]`. No roles table, no UI editor, no custom roles. Workspace Admin assigns any of the 5 profiles. Last Workspace Admin can't self-demote. No user can assign a profile wider than their own. All assignments to `audit_events`. Superadmin grant also audited. |
| 12 | `megadocumento` §14 | **Content immutability after export** | No automatic content modification after exporting to Jira | Sync back from Jira? Two-way sync? Divergence handling beyond the current "new issue on re-export" rule | 🟢 resolved 2026-04-14 — **upsert-by-key + user-initiated import**. Outbound: re-export UPDATEs same issue via `jira_issue_key`, with `updated_at` check + user warning on external edits. Inbound: no auto-sync; user-initiated `import-from-jira` action creates a `work_item` from a Jira issue (`imported_from_jira`, `jira_source_key`). Exporting an imported item closes the loop by upserting the original issue. |
| 13 | `tasks/EP-00/design.md` §JWT signing | **JWT algorithm** | HS256 symmetric with secret rotation; RS256 deferred | Key management, JWKS endpoint, frontend verification without shared secret, migration plan for existing tokens | 🟢 resolved 2026-04-14 — **HS256** with secret in env var + documented rotation. Single backend, single frontend consumer — RS256 aporta nothing here. Migration to RS256 is mechanical if a second service ever consumes tokens. |
| 14 | `tasks/EP-00/specs/bootstrap/spec.md` §Open Qs + `tasks/EP-00/tasks-frontend.md` | **First-login flow** | Auto-create personal workspace on unknown user; no invite system; no `returnTo` preservation on redirect-to-login | Onboarding flow, invitation system, SSO domain matching, post-login deeplink UX | 🟢 resolved 2026-04-14 — **no auto-create**. Login resolves memberships: 0 → block with "contact admin"; 1 → land directly; N → workspace picker, last choice persisted in session. Workspace creation: Superadmin only. Team/Project creation: Workspace Admin / Project Admin in their workspace + Superadmin cross-workspace. `returnTo` deeplink preserved across login redirect. |
| 15 | `tasks/EP-01/design.md` §Rejected alternatives + `specs/work-item-model/spec.md` | **Work-item derivations & events** | `derived_state` computed on read (no cache); no event sourcing for transitions; no `due_date` enforcement | List-view perf at scale, time-travel queries, SLA/reminders on due dates | 🟢 resolved 2026-04-14 — **`derived_state` materialized write-through** (column on `work_items`, updated on transition/validation/review events). **No event sourcing** (timeline_events covers audit). **`due_date` added** (nullable, optional per item — enables deadline alerts, planning). |
| 16 | `tasks/EP-02/design.md` §Templates | **Template shape** | Markdown scaffold in description only; no JSON-schema typed templates | Per-type custom fields, field-level prepopulation, template versioning | 🟢 resolved 2026-04-14 — **JSON-schema typed templates, three layers**: (1) universal sections (Contexto/Objetivo/Alcance/Criterios/Dependencias/Validaciones/Desglose/Ownership) — inmutable engine; (2) field-type primitives (text/string/enum/multi_enum/date/date_range/duration/reference/user_reference/attachment_list) with `required`/`prefill`/`help_text`/`validation` — inmutable engine; (3) per-type concrete templates (Bug/Story/Spike/Milestone/Idea/Mejora/Tarea/Iniciativa/Cambio/Requisito) — to be drafted in `tasks/EP-02/specs/templates/spec.md` during propagation. Custom templates per workspace editable via admin UI. |
| 17 | `tasks/EP-03/design.md` §PromptRegistry + `specs/conversation/spec.md` | **Conversation threads + prompts** | No thread quota per user; no hot-reload of prompts (startup-only); no real-time collaborative threads | Rate limiting, ops-time prompt swap, live multi-user chat | 🟢 resolved 2026-04-14 — **REVISED after Dundun context**. Prompts moot (owned by Dundun/LangSmith). **No thread quota**. **Chat single-user** (threads are per `user_id`, no shared assistant). Threads: one `dundun_conversation_id` per user-thread; a thread can optionally reference a `work_item_id`. Our DB stores pointer + last-message cache; full history fetched from Dundun. |
| 18 | `tasks/EP-03/tasks-frontend.md` §SuggestionPreviewPanel | **Suggestion diff UI** | Highlight entire proposed block, no diff library | Inline/word-level diff, visual parity with VCS diff expectations | 🟢 resolved 2026-04-14 — **full diff viewer as core product interaction**. Stack: `diff-match-patch` (word-level algo) + `remark`/`remark-gfm`/`remark-parse` (markdown AST diff for section-aware semantics) + `react-markdown` (rendered, not raw) + custom React components. Features: unified inline (default) + side-by-side toggle (auto if >30% changed), section summary with jump links, minimap sidebar, **granular accept/reject per segment** (not all-or-nothing), LLM reasoning sidebar anchored to segments, keyboard nav (j/k/a/r), undo via EP-07 versioning. Effort: 1-2 weeks FE, justified because this is the core LLM loop. |
| 19 | `tasks/EP-04/design.md` §3.2 + §6 + §8 + `tasks-frontend.md` | **Completeness engine scope** | Score is 0/1 binary (no partial); `workspace_validator_roles` is YAML/env not DB; prompts as Python constants; section-version archive job deferred; LLM generation synchronous | Partial scoring UX/semantics, runtime config of roles, prompt hot-swap, storage growth on sections, async LLM promotion to Celery | 🟢 resolved 2026-04-14 — **REVISED after Dundun context**. **Score granular 0-100%** (weighted per universal section + required fields). **Validator roles in DB** (`validation_rules` table, admin UI editable). **Prompts moot** (owned by Dundun). **Section-version archive job (Celery)**. **Spec generation async**: Celery job POSTs Dundun `/chat` with agent=spec-gen + callback_url, Dundun returns 202, callback endpoint completes job. |
| 20 | `tasks/EP-05/design.md` §1.1 + §1.3 + §7 + §10 + `specs/dependencies/spec.md` + `tasks-frontend.md` + `tasks-backend.md` | **Task breakdown constraints** | No reverse FSM transitions; intra-work-item dependencies only (no cross-item); LLM breakdown synchronous; native drag events only (no DnD lib, mobile unsupported); READ COMMITTED acceptable for split/merge concurrency | Reverse-transition UX (undo "done"), cross-work-item dependency graphs, mobile reorder UX, LLM latency promotion to Celery, isolation upgrade under edit contention | 🟢 resolved 2026-04-14 — **REVISED after Dundun context**. **No reverse FSM** (explicit "unmark done"). **Cross-work-item dependencies yes**. **Breakdown async via Dundun**: Celery job POSTs Dundun with agent=breakdown + callback_url. **`dnd-kit`**. **READ COMMITTED default**; upgrade on demand. |
| 21 | `tasks/EP-06/design.md` §validation_requirements + §review linkage + §Cross-Epic | **Review & validation config** | `validation_requirements` seeded at deploy, not editable via API; optional rule-selection at review-request time (no rules engine); direct `WorkItemService.transition_state()` cross-epic call instead of event-driven | Admin UI for rules, automatic rule routing, event-bus decoupling to prevent cross-epic breakage | 🟢 resolved 2026-04-14 — **`validation_requirements` editable via API + admin UI** (CRUD on rules: activate/deactivate/tune per workspace/project/type). **Rule-selection at review-request opcional**: workspace/project baseline applies by default; owner can **add** additional rules per request but cannot remove mandatory ones. **Cross-epic calls direct + event emitted**: `WorkItemService.transition_state()` called directly for transactional correctness; event `review.completed` fires on in-process bus for EP-07 timeline + EP-08 notification (observational only, not part of critical path). |
| 22 | `tasks/EP-07/design.md` §storage + §diff + §outbox + `specs/versioning/spec.md` Out-of-Scope + `specs/timeline/spec.md` Out-of-Scope + `specs/comments/spec.md` Out-of-Scope | **Versioning + timeline + comments scope** | Full snapshot with TOAST (no delta); `difflib` only (no Markdown-aware); dual-write to `timeline_events` without outbox; no permanent version deletion; system-reserved `manual` trigger (no user-authored versions); no branch/fork/merge/tagging; no real-time timeline push, per-user timeline, export, or search; no reactions, mentions, comment search, or comment version history | Storage cost at 10x, Markdown diff quality, reliability of fan-in on failures, version branching/tagging, collaboration features (reactions/mentions), comment edit audit, timeline richness | 🟢 resolved 2026-04-14 — **Versioning**: full snapshot JSONB + TOAST; Markdown-aware diff (same stack as #18 — `remark` AST + `diff-match-patch`); permanent delete allowed for admin (audited); user-authored versions with optional commit message; lightweight version tagging (e.g. `pre-jira-export`); no branch/fork/merge (linear). **Timeline**: outbox pattern (dual-write within transaction + Celery worker publishes to bus — prevents loss on crash); SSE real-time push (reuses presence channel from B); per-user timeline view; JSON/CSV export; timeline search delegated to Puppet RAG. **Comments**: reactions (👍 ❤️ 🎉 🚀 👀 😅 minimum); @mentions with autocomplete + notification; comment search via RAG; comment edit history preserved (prior version stored, "edited" marker in UI, click to view history). |
| 23 | `tasks/EP-08/design.md` §event bus + §notifications | **Notifications delivery** | In-process event bus (no broker), no email or push (internal/SSE only), teams hard-deleted (no soft delete) | Cross-service fan-out, email/push channels, digest emails, team recovery/audit | 🟢 resolved 2026-04-14 — **event bus**: `AbstractEventBus` interface + `InProcessEventBus` default impl (DI-swappable to `RedisEventBus` if multi-service ever emerges). **No email/push** (confirmed per F1 — in-app only). **Teams soft-delete**: `teams.deleted_at`; team hidden from active listings/assignments but historical references intact. Admin can restore or permanently purge (audited). |
| 24 | `tasks/EP-09/design.md` §sort/count/cache/FTS + `specs/search/spec.md` | **Search & dashboards scope** | No prefix matching; eventual consistency on comment indexing; on-demand COUNT(*) per list; no materialized views (Redis cache only); PG FTS only (no Elasticsearch) | Type-ahead/prefix search UX, realtime search index, count perf at 10x, materialized rollups for dashboards, semantic search via external engine | 🟢 resolved 2026-04-14 — **search via Puppet only** (see #4). **Prefix matching yes** (handled by Puppet). **Saved searches yes** (local meta + Puppet query). COUNT on demand in SQL (add index to accelerate). No materialized views (Redis cache enough). EP-09 `specs/search/spec.md` rewrites to delegate to EP-13 Puppet client; drop FTS sections. |
| 25 | `tasks/EP-10/design.md` §RBAC + §audit + §dashboard + §credentials + §14 Out-of-Scope + `specs/integration/spec.md` | **Admin surface scope** | Capability `text[]` instead of RBAC tables; synchronous audit writes (no partitioning); on-demand dashboard queries (no mv); Fernet-encrypted credentials column (no vault); no key rotation; workspace-only Jira configs (no project-scoped UI); no role templates, no bulk CSV import, no webhook notifications | Role hierarchies/inheritance, audit partitioning & archive, mv-backed dashboards, external secrets vault, key-rotation lifecycle, project-scoped integrations, admin UX breadth | 🟢 resolved 2026-04-14 — **no RBAC tables** (see #11). **No audit partitioning** (small scale, see #7). **No materialized views** (Redis cache enough). **Fernet credentials kept** (no external vault — internal product). **Jira configs project-scoped** (product value). **No role templates, no bulk CSV, no webhook notifications**. |
| 26 | `tasks/EP-11/design.md` §12 + §13 Out-of-Scope + `specs/export/spec.md` + `specs/sync/spec.md` + `proposal.md` | **Jira export & sync scope** | Re-export creates new issue (no update); polling-only sync (no webhooks); fixed 15-minute sync cadence; no two-way sync; no bulk-export UI; no custom-field mapping UI; no platform→Jira transitions; workspace-only configs (no project scope) | Update-existing-issue semantics, webhook receiver + signature verification, per-workspace cadence, bidirectional sync, enterprise admin UX | 🟢 resolved 2026-04-14 — **outbound: upsert-by-key (update same issue on re-export)**; **inbound: user-initiated import action only (no polling/webhooks)**. New endpoint `POST /work-items/import-from-jira` pulls a Jira issue into a new `draft` work_item with `imported_from_jira`+`jira_source_key`. Drop: `sync_logs`, polling cadence, webhook receivers. EP-11 rewrites: `specs/sync/spec.md` → delete or repurpose to `specs/import/spec.md`. |
| 27 | `tasks/EP-12/specs/observability/spec.md` + `tasks/EP-12/design.md` + `tasks-backend.md` + `tasks.md` | **Observability backend** | Self-hosted monitoring (no paid APM); Postgres append-only `product_events` table instead of PostHog/Segment; 10% trace sampling | Migration to managed APM, analytics UX parity, sampling strategy under load | 🟢 resolved 2026-04-14 — **observability fully deferred**. Drop: Prometheus, Grafana, Loki, OpenTelemetry, Sentry, `product_events` table, trace sampling, health dashboards, LLM token metrics. **Keep**: Python stdlib `logging` to stdout (debugging via `docker logs`) + `CorrelationIDMiddleware` (UUID per request in log context). EP-12 narrows to "Responsive + Security + Performance". Tradeoff accepted: production debugging is ssh+grep; silent errors in Celery can go unnoticed; no automated LLM cost tracking. To revisit when scale or ops needs change. |
| 28 | `tasks/EP-13/design.md` §12 Decisions + §14 Out-of-Scope | **Search/RAG scope** | RRF static weights (no learned re-ranker); EN-only; no real-time index updates, saved searches, faceted semantic search, or per-workspace embedding config | Multi-language, learned fusion with training loop, UX parity with modern search, customization per workspace | 🟢 resolved 2026-04-14 — EP-13 reshapes into **PuppetClient (search) + push pipeline (ingestion)**. Our BE calls Puppet directly with tag `wm_<workspace_id>`. Add: real-time index on write (Celery push pipeline), saved searches (local `saved_searches` table + Puppet query params), prefix/type-ahead, faceted. Drop hybrid RRF (no local FTS). No per-workspace embedding config (Puppet handles). Multi-language deferred. |
| 29 | `tasks/EP-16/design.md` §decisions + `specs/gallery/spec.md` + `specs/security/spec.md` | **Attachments scope** | No PDF thumbnails; no real-time scan-status push (5s polling); no S3-side IAM-session-tag revocation | PDF preview, websocket/SSE push for scan state, tighter presigned-URL revocation window | 🟢 resolved 2026-04-14 — **context: app runs on internal Tuio VPN, threat model is insider-risk only**. **PDF thumbnails yes** (`pdf2image` + Pillow, Celery async). **ClamAV dropped entirely** (no virus scan, no Celery scan queue, no `attachment_scan_status` table, no SSE scan push). **Signed URLs dropped**: replaced by authenticated backend endpoint `GET /api/v1/attachments/:id/download` that verifies JWT + workspace membership + `can_view_attachment` capability, then streams from object storage. **Kept**: content-type validation, file size limits per user/workspace, upload rate limits, audit of sensitive uploads/downloads. EP-16 simplifies significantly. |
| 30 | `tasks/EP-17/proposal.md` + `tasks/EP-17/design.md` + `tasks/EP-17/tasks-backend.md` | **Edit locking scope** | Work-item-level locks only (no section-level); no locks on comments; locks apply to state transitions; lock-event repo has no read methods (admin tools read directly) | Parallel section editing UX, comment-edit concurrency, transition-conflict UX, admin tooling API surface | 🟢 resolved 2026-04-14 — **section-level locks** (parallel edit of different sections allowed). **No locks on comments** (append-only; own comment edit doesn't need lock). **Locks apply to state transitions** (can't transition to Ready while required sections are locked by others). **Lock-event repo exposes read methods** (admin panel lists active locks, force-unlock is audited). Heartbeat every 30s, 5-min inactivity auto-release, unlock-request notifies holder, force-unlock requires Workspace Admin / Superadmin. Presence (B) and locks share the same SSE channel but are separate concerns (ephemeral vs transactional). |

---

| 31 | (new, from conversation) | **Superadmin scope + bootstrap mechanism** | Not explicitly decided in original plan (Q9 had CLI; scope undefined) | API + UI surface needed. Security invariants around promotion. Audit trail | 🟢 resolved 2026-04-14 — **No CLI. Config seed** (`SEED_SUPERADMIN_EMAILS` env var or migration fixture). First Google OAuth login of a seed email creates `users` row with `is_superadmin = true`, pinned to `google_sub`. Scope (MVP of superadmin surface): create workspace + assign initial Workspace Admin · list workspaces globally · list users globally · cross-workspace audit (read-only) · global health dashboard · promote another user to superadmin (audited). Out for now: suspend workspaces, force-unlock EP-17, impersonate, bulk migration, cross-workspace owner reassignment. EP-10 needs extension for this API+UI surface. |
| 32 | (new, from conversation) | **Dundun integration architecture** | Originally plan had LiteLLM in-app (decision E) — replaced after dundun context | Client shape, auth, proxy vs direct, async callback pattern, conversation storage model | 🟢 resolved 2026-04-14 — **Dundun as the AI layer**. Our BE has `DundunClient` (HTTP + WS). All chat / gap detection / spec gen / suggestion / breakdown delegated to Dundun agents (agents owned by Dundun team — transparent to us). Auth: service key + `caller_role=employee` + `user_id`. Sync interactive chat: our BE proxies WS (FE → our WS → dundun `/ws/chat`) enforcing JWT + membership. Async AI tasks: Celery job POSTs Dundun with `callback_url`, dundun returns 202, callbacks to `/api/v1/dundun/callback`, job completes. Conversation state owned by Dundun (Temporal); our `conversation_threads` stores `dundun_conversation_id` + `last_message_preview` cache only. Prompts owned by Dundun/LangSmith. Search NOT via Dundun (see #4 — direct to Puppet). |

---

## Related but out-of-scope for this doc

The following are not MVP-shortcut decisions but surfaced during the MVP-label review. They belong in the next consistency-review pass:

- 20 cross-epic consistency issues in `tasks/consistency_review.md` (7 must-fix, 10 should-fix, 3 nitpick)
- 9 open product questions in `assumptions.md` §"Open Product Questions"
- LLM cost/evals/prompt-versioning plan absent from all planning artifacts
- Canonical schema owner for `work_items` not assigned (causing naming drift: `workspace_memberships` vs `workspace_members`, etc.)
